# Block Google Image Search from indexing any images
User-agent: Googlebot-Image
Disallow: /

# Allow regular Googlebot to crawl the site (for SEO)
User-agent: *
Allow: /
Disallow: /case-studies/carv
Disallow: /case-study-locked

# Note: Image indexing is also blocked via X-Robots-Tag headers
# in next.config.ts, which adds "noimageindex" to all /images/ requests
#
# The password-protected Carv case study is additionally blocked via
# X-Robots-Tag headers (see next.config.ts) and, more importantly,
# actually unreachable without a valid auth cookie (see src/middleware.ts) -
# this Disallow rule is defense-in-depth, not the real protection.

